# =============================================================
# Estimación de Pose 6-DoF — TFM UNIR 2026
# =============================================================
# Las dependencias se gestionan via pyproject.toml + uv.
# Este archivo existe para compatibilidad con pip y Google Colab.
#
# Instalación recomendada (local):
#   uv sync --all-extras
#
# Instalación alternativa (pip / Colab):
#   pip install -r requirements.txt
# =============================================================

# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
opencv-python>=4.8
Pillow>=10.0
imageio>=2.31

# Deep Learning
torch>=2.1
torchvision>=0.16
timm>=0.9
diffusers>=0.25

# 3D geometry & pose
pytransform3d>=3.0
trimesh>=4.0

# Experiment tracking & config
tensorboard>=2.14
wandb>=0.16
hydra-core>=1.3
omegaconf>=2.3

# Utilities
tqdm>=4.66
pyyaml>=6.0

# Aplicaciones interactivas
# Dashboard (Streamlit) — `streamlit run dashboard.py`
#   streamlit>=1.49 requerido por la API width="stretch"
streamlit>=1.49,<2
pandas>=2.0
# Demo de la charla (Gradio 6) — `python scripts/demo_charla.py`
gradio>=6,<7

# API REST del pipeline — `python scripts/api_server.py`
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0

# Análisis y figuras (experiments/, scripts/make_diagrams.py)
# graphviz (Python) requiere además el binario `dot` del sistema (brew install graphviz)
scikit-learn>=1.3
graphviz>=0.20
bop-bootstrap-ci>=0.1

# BOP evaluation (optional — needed for dataset eval)
# pycocotools>=2.0
# bop_toolkit_lib

# Jupyter / Colab (optional)
# jupyter>=1.0
# ipywidgets>=8.0
