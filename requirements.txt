# =============================================================
# Estimación de Pose 6-DoF — TFM UNIR 2026
# =============================================================
# Las dependencias se gestionan via pyproject.toml + uv.
# Este archivo existe para compatibilidad con pip y Google Colab.
#
# Instalación recomendada (local):
#   uv sync --all-extras
#
# Instalación alternativa (pip / Colab):
#   pip install -r requirements.txt
# =============================================================

# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
opencv-python>=4.8
Pillow>=10.0
imageio>=2.31

# Deep Learning
torch>=2.1
torchvision>=0.16
timm>=0.9
diffusers>=0.25

# 3D geometry & pose
pytransform3d>=3.0
trimesh>=4.0

# Experiment tracking & config
tensorboard>=2.14
wandb>=0.16
hydra-core>=1.3
omegaconf>=2.3

# Utilities
tqdm>=4.66
pyyaml>=6.0

# BOP evaluation (optional — needed for dataset eval)
# pycocotools>=2.0
# bop_toolkit_lib

# Jupyter / Colab (optional)
# jupyter>=1.0
# ipywidgets>=8.0
