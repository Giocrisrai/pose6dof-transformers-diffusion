# =============================================================
# Estimación de Pose 6-DoF — TFM UNIR 2026
# =============================================================

# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
opencv-python>=4.8
open3d>=0.17

# Deep Learning
torch>=2.1
torchvision>=0.16
timm>=0.9            # Vision Transformers (ViT, DINOv2)
diffusers>=0.25      # Diffusion models (Diffusion Policy)

# 3D geometry & pose
pytransform3d>=3.0   # SE(3), SO(3) transformations
trimesh>=4.0         # Mesh I/O and processing

# BOP Toolkit — evaluación oficial
bop_toolkit_lib      # VSD, MSSD, MSPD metrics

# Datasets & I/O
pycocotools>=2.0     # COCO-format annotations
Pillow>=10.0
imageio>=2.31

# Experiment tracking
tensorboard>=2.14
wandb>=0.16

# Utilities
tqdm>=4.66
pyyaml>=6.0
hydra-core>=1.3      # Config management
omegaconf>=2.3

# Jupyter / Colab
jupyter>=1.0
ipywidgets>=8.0

# ROS 2 (instalar via apt, no pip — solo referencia)
# ros-humble-desktop
# ros-humble-moveit
